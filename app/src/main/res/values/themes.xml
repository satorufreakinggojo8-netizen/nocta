<resources>
    <!-- Real theming lives in Kotlin (ui/theme/Theme.kt) via Compose;
         this XML theme only needs to exist as the Activity's window theme. -->
    <style name="Theme.Nocta" parent="android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
